<h1>Signup</h1>

<%= form_for :user do |f| %>
  Email : <%= f.email_field :email %>
  Name : <%= f.text_field :name %>
  Password : <%= f.password_field :password %>

  <%= f.submit 'Signup' %>
<% end %>
